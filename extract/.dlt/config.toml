[runtime]
log_level = "INFO"

[destination.duckdb]
# DuckDB destination configured via environment variables
# DUCKDB_DATABASE: path to local database file (e.g., ./data/schema_evolution.duckdb)
# See .env.example for details.
